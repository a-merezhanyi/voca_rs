extern crate voca_rs;

#[cfg(test)]

mod unit {
    mod case;
    mod chop;
    mod count;
    mod escape;
    mod index;
    mod manipulate;
    mod query;
    mod readme;
    mod split;
    mod strip;
    mod utils;
}
